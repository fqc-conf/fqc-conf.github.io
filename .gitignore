# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Test/build artefacts
.playwright-mcp/
*.log

# Site preview screenshots (kept locally, not in repo)
fqc-*.png

# LinkedIn carousel deliverables — keep local, never publish to the site
linkedin-carousel/

# Speaker/attendee working docs — contain attendee emails and order numbers.
# This repo is public: these must never be committed. Synced to HackMD instead.
talks-abstracts.md
lightning-talks.md

# Organising checklist — names speakers' accommodation and attendees' dietary
# needs, and is not public-facing. Keep it local alongside the docs above.
TODO.md

# Contains the HackMD note IDs. Those notes are readable by anyone with the
# link, so the IDs are effectively secrets — keep them out of a public repo.
sync-hackmd.sh

# Internal design/spec notes — describe infrastructure and where personal data
# lives. This repo is served publicly, so these must not be published.
docs/superpowers/

# Unrelated working directories that happen to live in this folder.
# This repo is the FQC workshop site only.
ccg-parser/
discoviz/
eiso/
hala/
lambeq/
mina/
winoground/
